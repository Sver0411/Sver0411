name: Generate pixel profile

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * *"
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Render Fuji and CRT cards
        uses: LuciNyan/pixel-profile/action@7a7937a63421dd6eba82aaae31f73e85df8d9f94
        with:
          outputs: |
            dist/github-stats?username=Sver0411&screen_effect=false&theme=fuji&dithering=true&hide=avatar
          crt_outputs: |
            dist/github-stats-crt?username=Sver0411&include_all_commits=true

      - name: Publish cards to output branch
        uses: crazy-max/ghaction-github-pages@c0d7ff0487ee0415efb7f32dab10ea880330b1dd
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
